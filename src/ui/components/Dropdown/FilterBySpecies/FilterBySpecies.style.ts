import styled from 'styled-components';

const StyledFilterBySpecies = styled.div`
  padding: 18px 14px;
  margin-top: 25px;
  background-color: #fff;
  border: 1px solid grey;
  border-radius: 6px;
  position: absolute;
  display: flex;
  flex-direction: column;
  gap: 10px;

  .poligon {
    position: absolute;
    top: -14px;
    left: 0;
  }
`;

export default StyledFilterBySpecies;
